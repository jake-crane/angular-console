export default interface Content {
  // id;
  device: string;
  imageUrl: string;
  xfRenderingServer: string;
  maxFileUploadSize/*: number*/;
  maxImageDpi/*: number*/;
  minImageDpi/*: number*/;
  maxImagePixels/*: number*/;
  minImagePixels/*: number*/;
}
